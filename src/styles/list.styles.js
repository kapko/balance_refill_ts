import { makeStyles } from '@material-ui/core/styles';

export const listStyles = makeStyles(() => ({
  root: {
    flexGrow: 1,
    maxWidth: 752,
  },
  title: {
    textAlign: 'center',
    fontSize: 32
  },
  demo: {
    fontSize: 32
  }
}));

